# useRequest

`useRequest` is a hook that allows you to make requests to a server and handle the response.

```jsx
import UserRequestDemo from "./useRequestDemo";

export default () => {

  return <UserRequestDemo />;
};
```
